package com.silverbp.android.ui.strength

import androidx.compose.ui.graphics.Color

/**
 * Yellow accent for the 已儲存 (favorite) tag/star — deliberately distinct from
 * the theme primary so a saved move reads as "bookmarked" rather than "active".
 */
val FavoriteTagColor = Color(0xFFFFC107)
