<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- LAN sync: NSD/Bonjour requires multicast on the Wi-Fi interface -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Chat: voice (Speech Recognizer) input -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <!-- Exercise / GPS path tracking -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.stepcounter" android:required="false" />

    <!-- Health Connect (matches iOS HealthKit toggle; toggled in Settings) -->
    <!-- BP is written (one-way mirror) so readings flow into Google Health;
         we don't read BP back yet, so no READ_BLOOD_PRESSURE. -->
    <uses-permission android:name="android.permission.health.WRITE_BLOOD_PRESSURE" />
    <!-- Blood glucose (血糖, v19) is likewise written one-way for the owner member
         only; no READ_BLOOD_GLUCOSE (we don't read external glucose back yet).
         ⚠️ Adding this permission requires re-filing the Play Console health-apps
         declaration before release — see RELEASE.md C-8. -->
    <uses-permission android:name="android.permission.health.WRITE_BLOOD_GLUCOSE" />
    <uses-permission android:name="android.permission.health.WRITE_EXERCISE" />
    <uses-permission android:name="android.permission.health.WRITE_EXERCISE_ROUTE" />
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />
    <uses-permission android:name="android.permission.health.READ_NUTRITION" />
    <!-- Nutrition (飲食) feature mirrors logged meals into Health Connect. -->
    <uses-permission android:name="android.permission.health.WRITE_NUTRITION" />
    <!-- Android 15+ (API 35/36): reading Health Connect from a background
         WorkManager job (sleep/nutrition/step backfill) returns nothing without
         this. Foreground reads work without it; this re-enables the workers. -->
    <uses-permission android:name="android.permission.health.READ_HEALTH_DATA_IN_BACKGROUND" />
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>

    <application
        android:name=".SilverBpApplication"
        android:allowBackup="true"
        android:backupAgent=".backup.SilverBpBackupAgent"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SilverBp"
        android:usesCleartextTraffic="false">

        <uses-native-library android:name="libOpenCL.so" android:required="false" />
        <uses-native-library android:name="libOpenCL-pixel.so" android:required="false" />
        <uses-native-library android:name="libOpenCL-car.so" android:required="false" />
        <uses-native-library android:name="libvndksupport.so" android:required="false" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <service
            android:name=".exercise.LocationTrackingService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Handles the "Mark as taken" action button on the medication
             reminder notification — fires synchronously, writes a dose row,
             and dismisses the notification without opening the app. -->
        <receiver
            android:name=".coach.MedicationActionReceiver"
            android:exported="false" />

        <!-- Health Connect rationale screen. HC's PermissionsActivity refuses
             to show the permission UI unless the requesting app declares a
             handler for these intents (modern action on Android 14+, legacy
             VIEW_PERMISSION_USAGE for the standalone HC app on Android 13). -->
        <activity
            android:name=".health.PermissionsRationaleActivity"
            android:exported="true"
            android:theme="@style/Theme.SilverBp">
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>
        <activity-alias
            android:name=".health.ViewPermissionUsageActivity"
            android:exported="true"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE"
            android:targetActivity=".health.PermissionsRationaleActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <!-- IME handling: adjustNothing tells the OS to NOT pan or resize the
             window when the keyboard appears. Compose then absorbs the IME
             inset via Modifier.imePadding() on the chat input bar. We tried
             every other combination on Vivo OriginOS and they all break:
               - unset / adjustUnspecified → adjustPan, which stacks with
                 imePadding to leave a keyboard-sized gap.
               - adjustResize → collapses the visible content area to zero
                 under edge-to-edge.
               - adjustPan → pushes TopAppBar off screen.
             adjustNothing + imePadding is the only combo that gives a clean
             layout across OEMs. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SilverBp"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>